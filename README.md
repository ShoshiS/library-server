# Book Server API

A simple RESTful API for managing a collection of books, built with **Express.js**.

## Features
- List books with pagination and search by name.
- Retrieve a single book by ID.
- Add a new book.
- Update a book (full update with PUT).
- Borrow a book by username (PATCH).
- Return a borrowed book (PATCH).
- Delete a book.